<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.content_manager_service.dao.ProjectIncentiveDao" >

    <insert id="createProjectIncentive" parameterType="com.model.ProjectIncentiveVO">
        INSERT INTO project_incentive
        (project_id,user_id,admin_id,total_hours,performance_bonus,client_referral,employee_referral,hot_opportunity,total_amount)
        VALUES
        (#{projectId},#{userId},#{admin_id},#{total_hours},#{performanceBonus},#{clientReferral},#{employeeReferral},#{hotOpportunity},#{totalAmount})
    </insert>

    <select id="getUserProjectIncentives" resultType="com.model.TaskReportVO">
        SELECT * FROM project_incentive WHERE project_id = #{projectId} AND user_id = #{userId}
    </select>


</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.user_manager_service.dao.UserDao" >

    <insert id="createGravitateUser" parameterType="com.model.UserVO" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO app_user
        (user_type,managed_by,role_id,is_admin,email,password,alternate_email,first_name,last_name,country,date_of_birth,phone_number,profile_picture_path,contract_path,employment_status,joining_date,billing,bank_name,account_number)
        VALUES
        (#{userType},#{managedBy},#{roleId},#{isAdmin},#{email},#{password},#{alternateEmail},#{firstName},#{lastName},#{country},#{dateOfBirth},#{phoneNumber},#{profilePicturePath},#{contractPath},#{employmentStatus},#{joiningDate},#{billing},#{bankName},#{accountNumber})
    </insert>

    <select id="checkIfUsernameExists" parameterType="String" resultType="int">
        SELECT CASE
        WHEN EXISTS(
            SELECT 1 FROM app_user
            WHERE
            email = #{username} OR alternate_email = #{username} LIMIT 1
        )
        THEN 1 ELSE 0
        END
    </select>
    
    <select id="getGravitateUserByUsername" resultType="com.model.UserVO" parameterType="String">
        SELECT * FROM app_user WHERE email = #{username} LIMIT 1
    </select>

    <delete id="deleteGravitateUser" parameterType="Long">
        DELETE FROM app_user WHERE user_id = #{userId}
    </delete>

    <update id="updateGravitateUserPassword" parameterType="com.model.UserVO">
        UPDATE app_user
        SET
        password = #{password}
        WHERE
        user_id = #{userId}
    </update>

    <select id="getAllGravitateUsers" resultType="Map">
        SELECT A.user_id,A.first_name,A.last_name,A.email,A.profile_picture_path,R.role_name,R.rolekra
        FROM
        app_user A
        INNER JOIN
        role R
        ON
        A.role_id = R.role_id
        WHERE user_type != 'Admin'
        <if test="search != null">
            <bind name="pattern" value="'%' + search + '%'"/>
            AND A.first_name LIKE #{pattern}
        </if>
        <if test="roleId != null">
            AND R.role_id = #{roleId}
        </if>
    </select>




</mapper>